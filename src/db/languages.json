{
    "maintenance": {
        "en": "the bot is on maintenance, please try later.",
        "fr": "le bot est en maintenance, merci de réessayer plus tard."  
    },

    "userMissingPermission": {
        "en": "you miss permissions to use this command!",
        "fr": "tu manques de permissions pour pouvoir utiliser cette commande !"
    },

    "botMissingPermission": {
        "en": "i miss these permissions to execute this command:",
        "fr": "je n'ai pas ces permissions pour pouvoir éxecuter cette commande :"
    },
    
    "commandNotInDM": {
        "en": "this command can't be used in DMs.",
        "fr": "cette commande ne peut pas être utilisée ici."
    },

    "channelNotNSFW": {
        "en": "you must be in an NSFW channel in order to use this command.",
        "fr": "tu dois être dans un salon NSFW pour utiliser cette commande."
    },

    "commandNotEnabled": {
        "en": "this command is disabled.",
        "fr": "cette commande est désactivée."
    },

    "cooldown": {
        "en": "this command is on cooldown!\nTime remaining: ",
        "fr": "cette commande est en cooldown !\nTemps restant : "
    },

    "help": {
        "title": {
            "en": "Help pannel",
            "fr": "Pannel d'aide"
        }
    },

    "commandArgs": {

        "argsMissing": {
            "en": "args are missing",
            "fr": "il manque des arguments"
        },

        "number": {

            "type": {
                "en": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* must be a **number**.",
                "fr": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* doit être un **nombre**."
            },
            "min": {
                "en": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* must be **${argConfig.params.min}** at minimum.",
                "fr": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* doit valoir **${argConfig.params.min}** au minimum."
            },
            "max": {
                "en": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* must be **${argConfig.params.max}** at maximum.",
                "fr": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* doit valoir **${argConfig.params.max}** au maximum."
            },
            "int": {
                "en": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* must be an **integer**.",
                "fr": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* doit être un **entier**."
            }
        },

        "string": {

            "type": {
                "en": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* must be a **string**.",
                "fr": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* doit être du **texte**."
            },
            "equalsTo": {
                "en": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* must be equal to: **${argConfig.params.equalsTo.slice(0, -1).join('**, **')}** or **${argConfig.params.equalsTo.slice(-1)}**.",
                "fr": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* doit être égal à : **${argConfig.params.equalsTo.slice(0, -1).join('**, **')}** ou **${argConfig.params.equalsTo.slice(-1)}**."
            },
            "length": {
                "en": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* must be **${argConfig.params.length}** characters or less.",
                "fr": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* doit être composé de **${argConfig.params.length}** caractères au maximum."
            }
        },

        "mention": {

            "type": {
                "en": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* must be a **mention**.",
                "fr": "*${typeof argConfig.name === 'object' ? argConfig.name[la] : argConfig.name}* doit être une **mention**."
            }
        }
    }
}